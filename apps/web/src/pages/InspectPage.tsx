import PlaceholderPage from "../components/PlaceholderPage.js";

export default function InspectPage() {
  return (
    <PlaceholderPage
      title="Inspect"
      description="Connect to an MCP server and interactively browse its capabilities. Send requests and inspect responses in real time."
    />
  );
}
