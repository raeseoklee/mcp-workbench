import PlaceholderPage from "../components/PlaceholderPage.js";

export default function ResourcesPage() {
  return (
    <PlaceholderPage
      title="Resources"
      description="List and read resources exposed by the server. Inspect URIs, MIME types, and raw content."
    />
  );
}
