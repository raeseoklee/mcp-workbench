import PlaceholderPage from "../components/PlaceholderPage.js";

export default function TestResultsPage() {
  return (
    <PlaceholderPage
      title="Test Results"
      description="View the results of your YAML test spec runs — passed, failed, skipped, and snapshot diffs — with drill-down into each assertion."
    />
  );
}
