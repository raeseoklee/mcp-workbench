import PlaceholderPage from "../components/PlaceholderPage.js";

export default function ToolsPage() {
  return (
    <PlaceholderPage
      title="Tools"
      description="Discover all tools advertised by the server, view their input schemas, and invoke them with custom arguments."
    />
  );
}
