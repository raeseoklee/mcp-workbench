import PlaceholderPage from "../components/PlaceholderPage.js";

export default function TimelinePage() {
  return (
    <PlaceholderPage
      title="Timeline"
      description="A chronological log of every JSON-RPC event in the session — requests, responses, and notifications with full payloads."
    />
  );
}
